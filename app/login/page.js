export const metadata = {
  title: "Login - Pos Imam",
  description: "Halaman login aplikasi POS Imam",
  icons: {
    icon: "/favicon.ico",
    shortcut: "/favicon.ico",
    apple: "/favicon.ico",
  },
};

import LoginClient from "./LoginClient";

export default function LoginPage() {
  return <LoginClient />;
}
